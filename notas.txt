16-03-2025
-- En el main crear un modelo de catalogos para poder tener registro de los catalogos agregados y asi poder generar su permiso general.
---Datos del modelo
----Id generico de Django
----Nombre_Modelo - El nombre de la clase en el modelo
----Nombre - El nombre del catalogo
----Descripcion - Funciona principal y notas generales (Como relaciones, especificaciones, validaciones, etc.)
-- En el archivo de migracion, usar el JSON que esta en el data para poder llenar la tabla nueva de catalogos y la tabla de procesos para asi poder tener una mejor relacion de los procesos.

#Ejemplo
# Generated by Django 5.1.7 on 2025-03-16 07:06

from django.db import migrations
from django.contrib.contenttypes.models import ContentType
from django.contrib.auth.models import Permission
# App-Catalogos
from catalogo.models import Estatus_Unidad
# App-Main
from main.models import Procesos

def create_procesos_app_catalogos(apps, schema_editor):
    # Se obtienen los modelos
    content_Estatus = ContentType.objects.get_for_model(Estatus_Unidad)
    
    # Se obtienen los permisos del modelo
    permisos_Estatus = Permission.objects.filter(content_type=content_Estatus)

    # Se recorren los permisos para agregarlos al objeto
    nCount = 1
    for objPermiso_Estatus in permisos_Estatus:
        Procesos.objects.create(
            id_proceso=nCount,
            nombre_proceso = objPermiso_Estatus.codename,
            descripcion_proceso = objPermiso_Estatus.name,
            tipo_proceso = 'catalogo'
        )
        nCount = nCount + 1

class Migration(migrations.Migration):

    dependencies = [
        ('main', '0001_initial'),
    ]

    operations = [
        migrations.RunPython(create_procesos_app_catalogos)
    ]
